/*
 * Update the text color of the header element
 * to red using vanilla JS only after the page
 * is fully loaded.
 */
document.addEventListener('DOMContentLoaded', () => {
  document.querySelector('header').style.color = '#FF0000';
});
